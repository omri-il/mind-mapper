# Copy to server/.env (gitignored) and fill in. Never commit the real key.
GEMINI_API_KEY=your-gemini-key-here
GEMINI_MODEL=gemini-3.5-flash
GEMINI_FALLBACK_MODEL=gemini-2.5-flash
PORT=5055
# Extra CORS origins (comma-separated), in addition to the built-in github.io + localhost:
# ALLOWED_ORIGINS=https://mind.omri-iram.co.il
